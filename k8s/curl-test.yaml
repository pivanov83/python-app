apiVersion: v1
kind: Pod
metadata:
  name: curl-test
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:latest
      command: ["sleep"]
      args: ["3600"]
